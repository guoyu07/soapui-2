{
  Date date=new Date();
  String newString=new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(date);
  sessionId=String.format("AutoGeneratedSessionTimeStamp:%s:UserId:%s",newString,AnalyticsProvider.ActionDescription.getUserId());
}
