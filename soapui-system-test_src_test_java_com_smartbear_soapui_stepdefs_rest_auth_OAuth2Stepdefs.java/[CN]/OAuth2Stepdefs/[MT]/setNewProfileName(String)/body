{
  DialogFixture renameProfileDialog=FestMatchers.dialogWithTitle(ProfileSelectionForm.RENAME_PROFILE_DIALOG_TITLE).using(robot);
  renameProfileDialog.textBox().setText(newName);
  renameProfileDialog.button(buttonWithText("OK")).click();
}
