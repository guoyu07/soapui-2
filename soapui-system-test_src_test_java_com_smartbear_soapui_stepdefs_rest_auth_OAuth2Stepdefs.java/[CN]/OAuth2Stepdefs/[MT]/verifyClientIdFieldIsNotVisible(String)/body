{
  DialogFixture accessTokenFormDialog=findDialog(OAuth2AuthenticationInspector.ACCESS_TOKEN_FORM_DIALOG_NAME,robot);
  FestUtils.verifyTextFieldIsNotShowingInDialog(accessTokenFormDialog,fieldName);
}
