{
  JPanel panel=UISupport.createEmptyPanel(5,15,15,15);
  panel.add(BorderLayout.NORTH,UISupport.wrapInEmptyPanel(new JLabel("Installed Extensions"),BorderFactory.createEmptyBorder(0,0,5,0)));
  panel.add(BorderLayout.CENTER,new JScrollPane(new JTable(pluginsTableModel)));
  return panel;
}
