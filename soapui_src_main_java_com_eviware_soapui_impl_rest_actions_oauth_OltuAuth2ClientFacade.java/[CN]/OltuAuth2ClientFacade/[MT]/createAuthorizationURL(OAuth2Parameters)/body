{
  return OAuthClientRequest.authorizationLocation(profile.getAuthorizationUri()).setClientId(profile.getClientId()).setResponseType(CODE).setScope(profile.getScope()).setRedirectURI(profile.getRedirectUri()).buildQueryMessage().getLocationUri();
}
