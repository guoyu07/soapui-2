{
  return OAuthClientRequest.authorizationLocation(profile.getAuthorizationURL()).setClientId(profile.getClientId()).setResponseType(CODE).setScope(profile.getScope()).setRedirectURI(profile.getRedirectURL()).buildQueryMessage().getLocationUri();
}
