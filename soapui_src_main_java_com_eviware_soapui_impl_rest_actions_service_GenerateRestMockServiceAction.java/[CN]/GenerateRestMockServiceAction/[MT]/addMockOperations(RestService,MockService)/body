{
  for (  RestResource oneResource : restService.getAllResources()) {
    MockOperation mockOperation=mockService.addNewMockOperation(oneResource);
    if (mockOperation != null)     mockOperation.addNewMockResponse("Response 1");
  }
}
