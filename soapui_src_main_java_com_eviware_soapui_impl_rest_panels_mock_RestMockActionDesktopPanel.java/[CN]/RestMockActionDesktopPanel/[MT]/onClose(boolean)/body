{
  getModelItem().getMockService().getProject().removeProjectListener(projectListener);
  responseListModel.release();
  if (dispatcher != null)   dispatcher.releaseEditorComponent();
  return release();
}
