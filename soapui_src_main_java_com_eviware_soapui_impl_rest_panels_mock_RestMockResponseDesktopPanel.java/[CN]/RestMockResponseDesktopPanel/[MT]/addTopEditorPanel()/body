{
  HttpHeadersInspector inspector=new HttpHeadersInspector(new MockResponseHeadersModel((RestMockResponse)getModelItem()));
  JComponent component=inspector.getComponent();
  component.setMaximumSize(new Dimension(Short.MAX_VALUE,MAX_HEIGHT_TOP_PANEL));
  return component;
}
