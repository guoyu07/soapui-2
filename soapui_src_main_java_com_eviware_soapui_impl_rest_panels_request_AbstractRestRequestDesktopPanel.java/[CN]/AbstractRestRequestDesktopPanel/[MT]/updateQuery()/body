{
  String query=queryPanel.getText();
  RestParamsPropertyHolder propertyHolder=getRequest().getResource().getParams();
  if (!query.isEmpty())   RestUtils.extractParamsFromQueryString(propertyHolder,query.substring(1));
}
