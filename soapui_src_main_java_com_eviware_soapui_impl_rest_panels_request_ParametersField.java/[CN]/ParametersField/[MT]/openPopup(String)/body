{
  final RestParamsTable restParamsTable=new RestParamsTable(request.getParams(),false,new RestParamsTableModel(request.getParams(),RestParamsTableModel.Mode.MINIMAL),NewRestResourceActionBase.ParamLocation.RESOURCE,true,true);
  JTable innerTable=restParamsTable.getParamsTable();
  innerTable.addKeyListener(new KeyAdapter(){
    @Override public void keyPressed(    KeyEvent e){
      if (e.getKeyCode() == KeyEvent.VK_ESCAPE && popupWindow != null) {
        popupWindow.dispose();
      }
    }
  }
);
  showParametersTableInWindow(restParamsTable,selectedParameter);
}
