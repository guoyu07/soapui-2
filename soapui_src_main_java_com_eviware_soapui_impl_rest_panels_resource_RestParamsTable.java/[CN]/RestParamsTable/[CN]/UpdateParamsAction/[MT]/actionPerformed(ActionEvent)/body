{
  String str=UISupport.prompt("Enter new url below","Extract Params","");
  if (str == null)   return;
  try {
    RestUtils.extractParams(str,params,false,defaultParamLocation == ParamLocation.RESOURCE ? EnumSet.allOf(RestUtils.TemplateExtractionOption.class) : EnumSet.noneOf(RestUtils.TemplateExtractionOption.class));
  }
 catch (  Exception e1) {
    UISupport.showErrorMessage(e1);
  }
}
