{
  addParamAction=AddParamAction.builder().withSmallIcon("/add_property.gif").withShortDescription("Adds a parameter to the parameter table").forTable(paramsTable).withPropertyHolder(params).build();
  removeParamAction=new RemovePropertyAction(paramsTable,params,"Removes the selected parameter");
  updateParamsAction=new UpdateParamsAction();
}
