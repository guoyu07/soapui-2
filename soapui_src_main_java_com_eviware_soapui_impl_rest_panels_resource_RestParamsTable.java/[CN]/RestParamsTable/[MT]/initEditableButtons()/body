{
  addParamAction=AddParamAction.builder().withSmallIcon("/add_property.gif").withShortDescription("Adds a parameter to the parameter table").forTable(paramsTable).withParent(this).withPropertyHolder(params).build();
  removeParamAction=new RemoveParamAction();
  updateParamsAction=new UpdateParamsAction();
}
