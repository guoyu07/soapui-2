{
  if (getInterface() != null && (getInterface().isGenerated() || StringUtils.isNullOrEmpty(getInterface().getWadlUrl())))   return new GeneratedWadlDefinitionLoader(getInterface());
 else   return new InterfaceCacheDefinitionLoader(restServiceDefinitionCache);
}
