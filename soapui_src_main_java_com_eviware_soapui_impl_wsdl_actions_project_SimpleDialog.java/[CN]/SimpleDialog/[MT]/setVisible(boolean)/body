{
  init();
  if (b) {
    beforeShow();
  }
 else {
    beforeHide();
  }
  UISupport.centerDialog(this);
  super.setVisible(b);
  Analytics.trackActiveScreen(this.title);
  if (b) {
    afterShow();
  }
 else {
    afterHide();
  }
}
