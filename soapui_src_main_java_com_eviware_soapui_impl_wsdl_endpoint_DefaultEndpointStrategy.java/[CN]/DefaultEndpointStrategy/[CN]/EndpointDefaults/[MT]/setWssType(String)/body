{
  if (wssPasswordType == null || wssPasswordType.equals(WsdlRequest.PW_TYPE_NONE)) {
    if (endpointConfig.isSetWssType())     endpointConfig.unsetWssType();
  }
 else {
    endpointConfig.setWssType(wssPasswordType);
  }
}
