{
  if (result.getTestStep() != testStep)   throw new RuntimeException("Trying to add sample for false testStep [" + result.getTestStep().getName() + "], "+ "expecting ["+ testStep.getName()+ "]");
  if (results == null)   results=new ArrayList<TestStepResult>();
  results.add(result);
}
