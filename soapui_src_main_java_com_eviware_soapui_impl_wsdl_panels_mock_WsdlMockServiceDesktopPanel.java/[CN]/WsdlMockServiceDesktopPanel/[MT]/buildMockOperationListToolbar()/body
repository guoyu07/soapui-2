{
  JXToolBar toolbar=UISupport.createToolbar();
  toolbar.add(UISupport.createToolbarButton(SwingActionDelegate.createDelegate(AddNewMockOperationAction.SOAPUI_ACTION_ID,getModelItem(),null,"/mockOperation.gif")));
  return toolbar;
}
