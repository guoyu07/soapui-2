{
  MockResult mockResult=mockResponse.getMockResult();
  if (mockResult != null) {
    mockResult.getMockRequest().setRequestContent(xml);
    fireContentChanged();
  }
 else {
    fireContentChanged();
  }
}
