{
  if (cachedProxySelector == null) {
    ProxySelector proxySelector=proxySearch.getProxySelector();
    cachedProxySelector=new CachedProxySelector(proxySelector == null ? null : ProxyUtils.filterHttpHttpsProxy(proxySearch.getProxySelector()));
  }
  return cachedProxySelector;
}
