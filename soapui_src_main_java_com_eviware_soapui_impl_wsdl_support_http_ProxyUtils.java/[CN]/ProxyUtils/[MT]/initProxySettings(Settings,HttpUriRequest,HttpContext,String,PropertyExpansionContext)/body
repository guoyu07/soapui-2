{
  if (!proxyEnabled) {
    resetRoutePlanner();
    return;
  }
  if (autoProxy) {
    setAutomaticProxySettings();
  }
 else {
    setManualProxySettings(settings,httpMethod,httpContext,urlString,context);
  }
}
