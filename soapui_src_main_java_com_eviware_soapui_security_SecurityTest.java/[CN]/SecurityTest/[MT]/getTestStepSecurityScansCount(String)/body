{
  if (getSecurityScansMap().isEmpty()) {
    return 0;
  }
 else {
    if (getSecurityScansMap().get(testStepId) != null)     return getSecurityScansMap().get(testStepId).size();
 else     return 0;
  }
}
