{
  StringToStringMap params=new StringToStringMap();
  for (  Map.Entry<String,String> entry : parameters.entrySet()) {
    if (entry.getValue() != null)     params.put(entry.getKey(),entry.getValue());
  }
  return params;
}
