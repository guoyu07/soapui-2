{
  super(testStep,config,parent,icon);
  if (config.getConfig() == null || !(config.getConfig() instanceof InvalidSecurityScanConfig))   initInvalidTypesConfig();
 else   invalidTypeConfig=(InvalidSecurityScanConfig)config.getConfig();
}
