{
  this.properties=new HashMap<String,TestProperty>();
  for (  String key : properties.keySet()) {
    if (properties.get(key).isRequestPart())     this.properties.put(key,properties.get(key));
  }
}
