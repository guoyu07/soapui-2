{
  xmlBombPosition++;
  previous.setEnabled(true);
  attachementArea.setText(xmlBombList.get(xmlBombPosition));
  if (xmlBombPosition == xmlBombList.size() - 1)   setEnabled(false);
  current.setText("  current:" + xmlBombPosition);
}
