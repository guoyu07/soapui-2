{
  String selectedAuthProfile=request.getSelectedAuthProfile() == null ? NO_AUTHORIZATION : request.getSelectedAuthProfile();
  profileSelectionComboBox.setSelectedItem(selectedAuthProfile);
  return outerPanel;
}
