{
  try {
    if (initFromCommandLine(args,true)) {
      if (run()) {
        return NORMAL_TERMINATION;
      }
    }
  }
 catch (  Throwable e) {
    log.error(e);
    SoapUI.logError(e);
  }
  return ABNORMAL_TERMINATION;
}
