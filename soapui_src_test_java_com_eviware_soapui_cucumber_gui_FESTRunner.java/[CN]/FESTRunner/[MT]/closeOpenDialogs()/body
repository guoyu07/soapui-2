{
  try {
    DialogFinder dialog=findDialog(new GenericTypeMatcher<Dialog>(Dialog.class){
      @Override protected boolean isMatching(      Dialog t){
        return t.isVisible();
      }
    }
).withTimeout(1000);
    dialog.using(getRobot()).close();
  }
 catch (  WaitTimedOutError e) {
  }
}
