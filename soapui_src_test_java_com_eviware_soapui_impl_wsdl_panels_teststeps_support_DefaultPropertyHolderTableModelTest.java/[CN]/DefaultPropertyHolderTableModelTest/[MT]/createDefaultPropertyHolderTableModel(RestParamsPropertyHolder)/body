{
  params.addProperty(FIRST_PARAM_NAME);
  DirectAccessPropertyHolderTableModel tableHolderModel=new DirectAccessPropertyHolderTableModel<RestParamsPropertyHolder>(params);
  tableHolderModel.setValueAt(PARAM_VALUE,0,1);
  return tableHolderModel;
}
