{
  RestParamProperty property=restRequest.getParams().getProperty(parameterName);
  if (property == null) {
    return false;
  }
  return parameterValue == null || parameterValue.equals(parameterValue);
}
