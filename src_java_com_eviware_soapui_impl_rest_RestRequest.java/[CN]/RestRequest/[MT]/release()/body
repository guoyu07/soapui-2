{
  super.release();
  params.release();
  if (getResource() != null)   getResource().removePropertyChangeListener(this);
  for (  RestRepresentation representation : representations) {
    representation.release();
  }
}
