{
  String old=getPath();
  if (getResource().getFullPath().equals(fullPath))   getConfig().unsetFullPath();
 else   getConfig().setFullPath(fullPath);
  notifyPropertyChanged("path",old,fullPath);
}
