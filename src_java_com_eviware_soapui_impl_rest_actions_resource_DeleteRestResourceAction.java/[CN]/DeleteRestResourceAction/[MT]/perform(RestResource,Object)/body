{
  if (UISupport.confirm("Delete Resource [" + resource.getName() + "] from ["+ resource.getResourceContainer().getName()+ "]","Delete Request")) {
    resource.getResourceContainer().deleteResource(resource);
  }
}
