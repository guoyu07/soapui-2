{
  if (UISupport.confirm("Delete Resource [" + resource.getName() + "] from ["+ resource.getResourceContainer().getName()+ "]","Delete Resource")) {
    resource.getResourceContainer().deleteResource(resource);
  }
}
