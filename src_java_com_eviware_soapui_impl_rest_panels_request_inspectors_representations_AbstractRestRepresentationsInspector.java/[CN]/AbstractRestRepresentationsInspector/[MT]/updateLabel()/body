{
  int cnt=0;
  for (  RestRepresentation representation : request.getRepresentations()) {
    if (types.contains(representation.getType()))     cnt++;
  }
  setTitle("Representations (" + cnt + ")");
}
