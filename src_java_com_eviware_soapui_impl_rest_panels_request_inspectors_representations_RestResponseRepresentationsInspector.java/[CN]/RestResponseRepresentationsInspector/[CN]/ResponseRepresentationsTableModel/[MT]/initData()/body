{
  data.clear();
  for (  RestRepresentation representation : request.getRepresentations(null,null)) {
    if (representation.getType() != RestRepresentation.Type.REQUEST)     data.add(representation);
  }
}
