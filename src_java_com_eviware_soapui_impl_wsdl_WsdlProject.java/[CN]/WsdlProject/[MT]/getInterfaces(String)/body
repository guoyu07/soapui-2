{
  ArrayList<AbstractInterface> result=new ArrayList<AbstractInterface>();
  for (  AbstractInterface iface : interfaces) {
    if (iface.getType().equals(type))     result.add(iface);
  }
  return result;
}
