{
  if (exitCode == 0)   UISupport.showInfoMessage("Execution finished successfully",context.getTitle());
 else   UISupport.showInfoMessage("Execution finished with errorCode " + exitCode + ",\r\nplease check log for error messages",context.getTitle());
}
