{
  if (toolsForm == null) {
    toolsForm=new SimpleForm();
    toolsForm.append("hermes-config.xml path",new DirectoryFormComponent("Location of desired HermesJMS configuration (hermes-config.xml)"));
  }
  return toolsForm;
}
