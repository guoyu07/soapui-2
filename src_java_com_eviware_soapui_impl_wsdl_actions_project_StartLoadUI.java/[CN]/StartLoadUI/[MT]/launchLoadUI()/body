{
  String loadUIBatPath=SoapUI.getSettings().getString(LoadUISettings.LOADUI_PATH,"") + File.separator + LOADUI+ (UISupport.isWindows() ? BAT : UISupport.isMac() ? COMMAND : SH);
  startLoadUI(loadUIBatPath);
}
