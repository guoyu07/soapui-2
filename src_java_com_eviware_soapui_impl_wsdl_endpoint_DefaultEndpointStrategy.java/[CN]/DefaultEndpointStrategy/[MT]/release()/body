{
  project.removeProjectListener(projectListener);
  for (  Interface iface : project.getInterfaceList())   iface.removePropertyChangeListener(WsdlInterface.ENDPOINT_PROPERTY,propertyChangeListener);
}
