{
  String oldContent=getResponseContent();
  if (responseContent.equals(oldContent))   return;
  this.responseContent=responseContent;
  notifyPropertyChanged(RESPONSE_CONTENT_PROPERTY,oldContent,responseContent);
}
