{
  if (mockServiceEndpoint != null)   return mockServiceEndpoint + getPath();
  String host=getHost();
  if (StringUtils.isNullOrEmpty(host))   host="127.0.0.1";
  return getProtocol() + host + ":"+ getPort()+ getPath();
}
