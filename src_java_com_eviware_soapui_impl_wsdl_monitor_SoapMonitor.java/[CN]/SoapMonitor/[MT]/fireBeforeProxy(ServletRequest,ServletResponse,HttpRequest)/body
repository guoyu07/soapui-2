{
  for (  MonitorListener listener : listeners.get()) {
    try {
      listener.beforeProxy(this,request,response,httpRequest);
    }
 catch (    Throwable t) {
      SoapUI.logError(t);
    }
  }
}
