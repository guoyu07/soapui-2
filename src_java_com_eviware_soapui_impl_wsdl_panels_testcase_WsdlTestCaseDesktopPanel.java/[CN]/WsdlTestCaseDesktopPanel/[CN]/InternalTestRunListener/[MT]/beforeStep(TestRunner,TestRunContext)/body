{
  if (SoapUI.getTestMonitor().hasRunningLoadTest(getModelItem()))   return;
  TestStep testStep=runContext.getCurrentStep();
  if (testStep != null)   testStepList.setSelectedValue(testStep,true);
}
