{
  addAssertionButton=createActionButton(SwingActionDelegate.createDelegate(AddAssertionAction.SOAPUI_ACTION_ID,getRequest().getTestStep(),null,"/addAssertion.gif"),true);
  JPanel panel=new JPanel(new BorderLayout());
  panel.add(super.buildToolbar(),BorderLayout.NORTH);
  JXToolBar toolbar=UISupport.createToolbar();
  addToolbarComponents(toolbar);
  panel.add(toolbar,BorderLayout.SOUTH);
  return panel;
}
