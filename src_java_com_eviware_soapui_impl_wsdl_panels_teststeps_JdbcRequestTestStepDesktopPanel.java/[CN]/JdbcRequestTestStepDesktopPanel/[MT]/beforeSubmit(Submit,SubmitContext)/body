{
  if (submit.getRequest() != jdbcRequestTestStep.getJdbcRequest())   return true;
  setEnabled(false);
  cancelButton.setEnabled(JdbcRequestTestStepDesktopPanel.this.submit != null);
  return true;
}
