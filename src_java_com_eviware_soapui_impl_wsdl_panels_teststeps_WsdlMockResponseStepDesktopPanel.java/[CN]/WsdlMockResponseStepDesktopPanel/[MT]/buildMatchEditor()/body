{
  JPanel panel=new JPanel(new BorderLayout());
  panel.add(UISupport.getEditorFactory().buildXmlEditor(new ModelItemPropertyEditorModel<WsdlMockResponseTestStep>(getModelItem(),"match")),BorderLayout.CENTER);
  return panel;
}
