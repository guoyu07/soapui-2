{
  JPanel panel=new JPanel(new BorderLayout());
  panel.add(UISupport.getEditorFactory().buildXPathEditor(new ModelItemPropertyEditorModel<WsdlMockResponseTestStep>(getModelItem(),"query")),BorderLayout.CENTER);
  return panel;
}
