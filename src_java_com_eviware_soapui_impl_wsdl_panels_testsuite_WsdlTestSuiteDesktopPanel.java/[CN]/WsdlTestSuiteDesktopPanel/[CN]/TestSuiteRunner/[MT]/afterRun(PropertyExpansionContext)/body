{
  super.afterRun(context);
  try {
    getModelItem().runTearDownScript(context);
  }
 catch (  Exception e) {
    SoapUI.logError(e);
  }
 finally {
    WsdlTestSuiteDesktopPanel.this.afterRun();
  }
}
