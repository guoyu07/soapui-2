{
  try {
    if (buffer == null) {
      arg0.write("\r\n".getBytes());
      ((MimeMultipart)message.getContent()).writeTo(arg0);
    }
 else     arg0.write(buffer);
  }
 catch (  Exception e) {
    SoapUI.logError(e);
  }
}
