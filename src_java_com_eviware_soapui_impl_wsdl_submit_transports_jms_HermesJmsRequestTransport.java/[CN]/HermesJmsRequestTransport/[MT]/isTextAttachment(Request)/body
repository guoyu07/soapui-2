{
  if (request.getAttachments().length > 0 && (request.getAttachments()[0].getContentType().contains("/text")) || request.getAttachments()[0].getContentType().contains("/xml")) {
    return true;
  }
  return false;
}
