{
  this.target=target;
  this.baseIcon=UISupport.createImageIcon(baseIcon);
  animateIcons=new ImageIcon[num];
  for (int c=0; c < animateIcons.length; c++)   animateIcons[c]=UISupport.createImageIcon(animationBaseIcon + "_" + (c + 1)+ "."+ type);
}
