{
  if (!(modelItem instanceof AbstractWsdlModelItem<?>))   return null;
  WsdlProject project=(WsdlProject)ModelSupport.getModelItemProject(modelItem);
  if (project == null)   return null;
  String docroot=project.getResourceRoot();
  if (!StringUtils.hasContent(docroot))   return new File("").getAbsolutePath();
  docroot=context == null ? PropertyExpansionUtils.expandProperties(modelItem,docroot) : PropertyExpansionUtils.expandProperties(context,docroot);
  return docroot;
}
