{
  path=path.toUpperCase();
  return path.startsWith("HTTP:/") || path.startsWith("HTTPS:/") || path.startsWith("/")|| path.startsWith("\\")|| (path.length() > 2 && path.substring(1,2).equals(":\\"));
}
