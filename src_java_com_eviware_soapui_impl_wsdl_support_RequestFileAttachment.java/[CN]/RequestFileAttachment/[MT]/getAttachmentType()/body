{
  if (getModelItem() == null || getPart() == null || getModelItem().getAttachmentPart(getPart()) == null)   return AttachmentType.UNKNOWN;
 else   return getModelItem().getAttachmentPart(getPart()).getAttachmentType();
}
