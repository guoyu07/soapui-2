{
  this.modelItem=modelItem;
  this.config=config;
  for (  PropertyConfig propertyConfig : config.getPropertyList()) {
    if (StringUtils.hasContent(propertyConfig.getName()))     addProperty(propertyConfig,false);
  }
}
