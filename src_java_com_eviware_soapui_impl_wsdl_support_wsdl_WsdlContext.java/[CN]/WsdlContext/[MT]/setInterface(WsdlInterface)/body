{
  if (this.iface == null && iface != null) {
    if (definition != null)     definitionCache.put(url,definition);
 else     loaded=false;
    if (schemaTypes != null)     schemaCache.put(url,schemaTypes);
 else     loaded=false;
  }
  this.iface=iface;
}
