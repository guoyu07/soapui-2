{
  this.config=config;
  this.container=container;
  this.label=label;
  if (config.getConfiguration() == null)   config.addNewConfiguration();
  load(new XmlObjectConfigurationReader(config.getConfiguration()));
}
