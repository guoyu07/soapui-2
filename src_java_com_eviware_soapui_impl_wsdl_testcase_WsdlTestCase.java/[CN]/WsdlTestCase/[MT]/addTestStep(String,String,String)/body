{
  TestStepConfig newStepConfig=((HttpRequestStepFactory)WsdlTestStepRegistry.getInstance().getFactory(type)).createNewTestStep(this,name,endpoint);
  if (newStepConfig != null) {
    return addTestStep(newStepConfig);
  }
 else   return null;
}
