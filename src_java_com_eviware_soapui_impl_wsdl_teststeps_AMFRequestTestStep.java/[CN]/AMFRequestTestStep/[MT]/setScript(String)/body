{
  if (scriptText.equals(this.script))   return;
  String oldScript=this.script;
  this.script=scriptText;
  scriptEngine.setScript(scriptText);
  notifyPropertyChanged("groovyScript",oldScript,scriptText);
  saveScript();
}
