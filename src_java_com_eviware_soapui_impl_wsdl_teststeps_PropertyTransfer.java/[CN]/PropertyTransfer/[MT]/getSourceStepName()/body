{
  if (sourceStep != null)   return sourceStep;
  if (testCase == null)   return null;
  TestModelItem step=testCase.findPreviousStepOfType(this.testStep,WsdlTestRequestStep.class);
  return step == null ? null : step.getName();
}
