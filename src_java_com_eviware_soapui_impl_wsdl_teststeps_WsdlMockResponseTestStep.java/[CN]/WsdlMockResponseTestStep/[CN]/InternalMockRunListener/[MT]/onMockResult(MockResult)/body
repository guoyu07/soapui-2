{
  if ((waiting || isPreStart()) && result.getMockResponse() == testMockResponse) {
    this.setResult((WsdlMockResult)result);
    if (waiting) {
synchronized (this) {
        notifyAll();
      }
    }
  }
}
