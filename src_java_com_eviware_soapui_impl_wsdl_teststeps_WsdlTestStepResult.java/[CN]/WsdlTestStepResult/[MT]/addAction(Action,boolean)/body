{
  if (isDiscarded())   return;
  if (actionList == null) {
    actionList=new DefaultActionList(testStep.getName());
  }
  actionList.addAction(action);
  if (isDefault)   actionList.setDefaultAction(action);
}
