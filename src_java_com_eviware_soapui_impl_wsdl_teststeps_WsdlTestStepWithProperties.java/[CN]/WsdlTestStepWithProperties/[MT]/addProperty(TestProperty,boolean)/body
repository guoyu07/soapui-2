{
  if (properties == null)   properties=new HashMap<String,TestProperty>();
  properties.put(property.getName().toUpperCase(),property);
  propertyList.add(property);
  if (notify) {
    firePropertyAdded(property.getName());
  }
}
