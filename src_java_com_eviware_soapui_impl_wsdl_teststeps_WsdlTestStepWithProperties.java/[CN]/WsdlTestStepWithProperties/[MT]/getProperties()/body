{
  Map<String,TestProperty> result=new HashMap<String,TestProperty>();
  for (  String name : properties.keySet())   result.put(properties.get(name).getName(),properties.get(name));
  return result;
}
