{
  if (UISupport.confirm("Remove assertion [" + target.getName() + "] from ["+ target.getAssertable().getModelItem().getName()+ "]","Remove Assertion")) {
    target.getAssertable().removeAssertion(target);
  }
}
