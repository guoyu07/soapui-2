{
  if (messageExchange.getTimeTaken() > Long.parseLong(SLA)) {
    throw new AssertionException(new AssertionError("Response did not meet SLA " + messageExchange.getTimeTaken() + "/"+ SLA));
  }
  return "Response meets SLA";
}
