{
  if (modelItem instanceof TestStep)   return ((TestStep)modelItem).getLabel();
 else   if (modelItem instanceof TestCase)   return ((TestCase)modelItem).getLabel();
  return modelItem.getName();
}
