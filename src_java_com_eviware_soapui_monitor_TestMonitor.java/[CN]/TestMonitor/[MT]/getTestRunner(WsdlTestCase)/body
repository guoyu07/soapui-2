{
  Iterator<TestRunner> iterator=runningTestCases.iterator();
  while (iterator.hasNext()) {
    TestRunner testRunner=iterator.next();
    if (testRunner.getTestCase() == testCase)     return testRunner;
  }
  return null;
}
