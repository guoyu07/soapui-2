{
  Iterator<LoadTestRunner> iterator=runningLoadTests.iterator();
  while (iterator.hasNext()) {
    if (iterator.next().getLoadTest().getTestCase() == testCase)     return true;
  }
  return false;
}
