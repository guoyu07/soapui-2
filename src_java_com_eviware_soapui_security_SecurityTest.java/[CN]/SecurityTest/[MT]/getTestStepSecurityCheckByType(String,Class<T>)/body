{
  List<T> result=new ArrayList<T>();
  for (  SecurityScan check : getTestStepSecurityChecks(testStepId))   if (securityCheckType.isAssignableFrom(check.getClass()))   result.add((T)check);
  return result;
}
