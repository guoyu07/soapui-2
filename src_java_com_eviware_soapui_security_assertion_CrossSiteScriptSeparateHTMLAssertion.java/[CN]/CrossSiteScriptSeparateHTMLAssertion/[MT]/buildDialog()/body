{
  dialog=ADialogBuilder.buildDialog(CrossSiteScripSeparateHTMLConfigDialog.class);
  dialog.setSize(600,600);
  dialog.getFormField(CrossSiteScripSeparateHTMLConfigDialog.GROOVY).setProperty("component",new JScrollPane(buildGroovyPanel()));
  dialog.getFormField(CrossSiteScripSeparateHTMLConfigDialog.GROOVY).setProperty("dimension",new Dimension(450,450));
}
