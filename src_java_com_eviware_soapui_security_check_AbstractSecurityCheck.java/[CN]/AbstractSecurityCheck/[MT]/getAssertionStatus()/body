{
  int cnt=getAssertionCount();
  if (cnt == 0)   return currentStatus;
  if (securityCheckResult.getStatus() == SecurityCheckStatus.OK)   currentStatus=AssertionStatus.VALID;
 else   currentStatus=AssertionStatus.FAILED;
  return currentStatus;
}
