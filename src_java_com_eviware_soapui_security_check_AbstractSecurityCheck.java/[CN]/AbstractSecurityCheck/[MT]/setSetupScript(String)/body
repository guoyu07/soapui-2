{
  if (getConfig().getSetupScript() == null)   getConfig().addNewSetupScript();
  getConfig().getSetupScript().setStringValue(text);
  if (setupScriptEngine != null)   setupScriptEngine.setScript(text);
}
