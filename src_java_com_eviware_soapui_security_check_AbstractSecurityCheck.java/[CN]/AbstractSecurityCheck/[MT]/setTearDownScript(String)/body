{
  if (config.getTearDownScript() == null)   config.addNewTearDownScript();
  config.getTearDownScript().setStringValue(text);
  if (tearDownScriptEngine != null)   tearDownScriptEngine.setScript(text);
}
