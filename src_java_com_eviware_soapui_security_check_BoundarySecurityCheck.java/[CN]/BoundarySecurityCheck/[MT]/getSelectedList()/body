{
  List<String> selected=new ArrayList<String>();
  for (  SecurityCheckedParameter cpc : parameterHolder.getParameterList()) {
    if (cpc.isChecked())     selected.add(cpc.getName());
  }
  return selected;
}
