{
  super(testStep,config,parent,icon);
  if (config.getConfig() == null || !(config.getConfig() instanceof InvalidSecurityCheckConfig))   initInvalidTypesConfig();
 else   invalidTypeConfig=(InvalidSecurityCheckConfig)config.getConfig();
}
