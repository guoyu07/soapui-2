{
  updateRequestContent();
  WsdlTestCaseRunner testCaseRunner=new WsdlTestCaseRunner((WsdlTestCase)this.testStep.getTestCase(),new StringToObjectMap());
  testStep.run(testCaseRunner,testCaseRunner.getRunContext());
}
