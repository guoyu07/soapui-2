{
  super(config,parent,icon);
  monitorApplicable=true;
  if (config == null) {
    config=SecurityCheckConfig.Factory.newInstance();
    parameterExposureCheckConfig=(ParameterExposureCheckConfig)config.changeType(ParameterExposureCheckConfig.type);
  }
 else {
    parameterExposureCheckConfig=(ParameterExposureCheckConfig)config.changeType(ParameterExposureCheckConfig.type);
  }
  minimumCharactersTextField=new JTextField(((ParameterExposureCheckConfig)config).getMinimumLength());
}
