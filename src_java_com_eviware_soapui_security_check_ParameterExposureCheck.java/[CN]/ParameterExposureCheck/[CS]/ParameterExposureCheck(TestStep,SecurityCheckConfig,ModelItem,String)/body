{
  super(testStep,config,parent,icon);
  if (config.getConfig() == null || !(config.getConfig() instanceof ParameterExposureCheckConfig))   initConfig();
 else   parameterExposureCheckConfig=(ParameterExposureCheckConfig)getConfig().getConfig();
}
