{
  super(testStep,config,parent,icon);
  if (config.getConfig() == null || !(config.getConfig() instanceof SQLInjectionCheckConfig))   initSqlInjectionConfig();
 else   sqlInjectionConfig=(SQLInjectionCheckConfig)getConfig().getConfig();
}
