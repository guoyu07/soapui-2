{
  super(testStep,config,parent,icon);
  if (config.getConfig() == null || !(config.getConfig() instanceof SQLInjectionCheckConfig))   initXPathInjectionConfig();
 else   xpathList=(XPathInjectionConfig)getConfig().getConfig();
}
