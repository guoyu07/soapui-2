{
  for (int c=0; c < monitorSecurityChecksList.size(); c++) {
    AbstractSecurityCheck securityCheck=getSecurityCheckAt(c);
    if (securityCheck.getName().equals(name))     return securityCheck;
  }
  return null;
}
