{
  String name=type;
  int numNames=0;
  for (  AbstractSecurityCheck existingCheck : monitorSecurityTest.getMonitorSecurityChecksList()) {
    if (existingCheck.getType().equals(name))     numNames++;
  }
  if (numNames != 0) {
    name+=" " + numNames;
  }
  return name;
}
