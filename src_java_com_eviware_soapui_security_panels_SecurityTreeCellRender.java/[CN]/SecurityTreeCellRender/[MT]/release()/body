{
  for (  DefaultMutableTreeNode key : componentTree.keySet())   if (key instanceof TestStepNode)   ((TestStepCellRender)componentTree.get(key)).release();
 else   ((SecurityCheckCellRender)componentTree.get(key)).release();
  componentTree.clear();
}
