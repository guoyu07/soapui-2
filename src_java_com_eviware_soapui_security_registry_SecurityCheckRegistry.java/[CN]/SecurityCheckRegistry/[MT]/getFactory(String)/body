{
  for (  String cc : availableSecurityChecks.keySet()) {
    SecurityCheckFactory scf=availableSecurityChecks.get(cc);
    if (scf.getSecurityCheckType().equals(type))     return scf;
  }
  return null;
}
