{
  for (  String cc : availableSecurityChecks.keySet()) {
    AbstractSecurityCheckFactory scf=availableSecurityChecks.get(cc);
    if (scf.getType().equals(type))     return scf;
  }
  return null;
}
