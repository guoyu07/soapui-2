{
  if (getConfig().getTearDownScript() == null)   getConfig().addNewTearDownScript();
  getConfig().getTearDownScript().setStringValue(text);
  if (tearDownScriptEngine != null)   tearDownScriptEngine.setScript(text);
}
