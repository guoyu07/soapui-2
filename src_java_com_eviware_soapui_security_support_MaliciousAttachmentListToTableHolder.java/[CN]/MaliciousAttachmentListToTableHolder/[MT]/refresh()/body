{
  if (filesList != null) {
    AttachmentElement item=filesList.getFirstItem();
    String label=(item != null) ? item.getAttachment().getName() : "";
    if (item != null) {
      load(item);
    }
  }
}
