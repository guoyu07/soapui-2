{
  List<SecurityCheckedParameter> paramsList=new ArrayList<SecurityCheckedParameter>();
  for (  SecurityCheckedParameter param : params)   if (param.getName().equals(paramName))   paramsList.add(param);
  return paramsList;
}
