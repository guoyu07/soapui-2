{
  ArrayList<SecurityCheckedParameter> paramsToRemove=new ArrayList<SecurityCheckedParameter>();
  for (  int index : selected)   paramsToRemove.add(params.get(index));
  for (  SecurityCheckedParameter param : paramsToRemove)   removeParameter(param);
}
