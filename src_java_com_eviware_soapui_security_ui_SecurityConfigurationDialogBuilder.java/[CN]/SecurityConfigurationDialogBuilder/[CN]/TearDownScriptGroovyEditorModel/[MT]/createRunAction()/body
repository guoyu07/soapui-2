{
  return new AbstractAction(){
    public void actionPerformed(    ActionEvent e){
      try {
        MockSecurityTestRunner securityTestRunner=new MockSecurityTestRunner((SecurityTest)((AbstractSecurityCheck)TearDownScriptGroovyEditorModel.this.getModelItem()).getParent());
        ((AbstractSecurityCheck)TearDownScriptGroovyEditorModel.this.getModelItem()).runTearDownScript(((AbstractSecurityCheck)TearDownScriptGroovyEditorModel.this.getModelItem()).getTestStep());
      }
 catch (      Exception e1) {
        UISupport.showErrorMessage(e1);
      }
    }
  }
;
}
