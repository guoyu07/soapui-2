{
  try {
    editorPane.setPage(new URL(url));
    return true;
  }
 catch (  NoRouteToHostException e) {
    SoapUI.log.warn("Failed to access [" + url + "]");
  }
catch (  Exception e) {
    SoapUI.logError(e);
  }
  return false;
}
