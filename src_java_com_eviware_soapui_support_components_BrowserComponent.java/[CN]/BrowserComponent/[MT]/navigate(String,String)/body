{
  if (errorPage != null)   setErrorPage(errorPage);
  this.url=url;
  if (browser == null) {
    initBrowser();
  }
  browser.navigate(getUrl());
  if (showingErrorPage)   showingErrorPage=false;
  return true;
}
