{
  if (SoapUI.isJXBrowserDisabled(true))   return;
  this.url=url;
  if (browser == null) {
    initBrowser();
  }
  configureBrowser();
  BrowserComponent.updateJXBrowserProxy();
  browser.navigate(url);
}
