{
  super("Prepared Properties (" + (model.getHeaders() == null ? "0" : model.getHeaders().size()) + ")","Prepared Properties amf message",true,PreparedPropertiesHeadersInspectorFactory.INSPECTOR_ID);
  this.model=model;
  model.addPropertyChangeListener(this);
}
