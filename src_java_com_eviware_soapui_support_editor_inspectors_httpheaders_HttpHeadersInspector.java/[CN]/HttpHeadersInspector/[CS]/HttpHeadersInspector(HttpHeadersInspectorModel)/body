{
  super("Headers (" + (model.getHeaders() == null ? "0" : model.getHeaders().size()) + ")","Additional HTTP Headers for this message",true,HttpHeadersInspectorFactory.INSPECTOR_ID);
  this.model=model;
  model.addPropertyChangeListener(this);
}
