{
  int pos=pt == null ? -1 : textField.pointToOffset(pt);
  if (pos == -1)   pos=textField.getCaretPosition();
  textField.setSelectedText(expansion.toString());
  if (pos >= 0) {
    textField.setCaretPosition(pos);
    textField.requestFocusInWindow();
  }
}
