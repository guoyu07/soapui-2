{
  if (footerCount == 0)   add(new JSeparator(),subMenus.size() + headerCount + 2+ (headerCount == 0 ? 0 : 1));
  add(footer,subMenus.size() + headerCount + footerCount+ 3+ (headerCount == 0 ? 0 : 1));
  footerCount++;
  return footer;
}
