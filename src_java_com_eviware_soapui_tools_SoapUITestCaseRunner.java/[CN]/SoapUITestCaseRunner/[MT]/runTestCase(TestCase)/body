{
  TestRunner testRunner=testCase.run(new StringToObjectMap(),testCase.getTestSuite().getRunType() == TestSuiteRunType.PARALLEL);
  if (testRunner.getStatus() == TestRunner.Status.RUNNING)   runningTests.add(testRunner);
}
