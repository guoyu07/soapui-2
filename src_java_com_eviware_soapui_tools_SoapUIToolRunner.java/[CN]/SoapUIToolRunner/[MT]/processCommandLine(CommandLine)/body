{
  setTool(cmd.getOptionValue("t"));
  if (cmd.hasOption("i"))   setInterface(cmd.getOptionValue("i"));
  if (cmd.hasOption("s"))   setSettingsFile(getCommandLineOptionSubstSpace(cmd,"s"));
  return true;
}
