{
  if (!listeners.isEmpty()) {
    DesktopListener[] array=listeners.toArray(new DesktopListener[listeners.size()]);
    for (    DesktopListener listener : array)     listener.desktopPanelClosed(desktopPanel);
  }
}
