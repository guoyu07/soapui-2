{
  super(new BorderLayout());
  this.modelItem=modelItem;
  propertyChangeListener=new InternalPropertyChangeListener();
  ((ModelItem)modelItem).addPropertyChangeListener(propertyChangeListener);
}
