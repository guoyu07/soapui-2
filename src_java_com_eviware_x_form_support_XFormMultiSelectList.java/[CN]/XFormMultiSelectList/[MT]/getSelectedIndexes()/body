{
  int cnt=0;
  for (int c=0; c < selected.size(); c++) {
    if (selected.get(c))     cnt++;
  }
  int[] result=new int[cnt];
  cnt=0;
  for (int c=0; c < selected.size(); c++) {
    if (selected.get(c))     result[cnt++]=c;
  }
  return result;
}
