{
  List<String> result=new ArrayList<String>();
  for (int c=0; c < selected.size(); c++) {
    if (selected.get(c))     result.add((String)listModel.get(c));
  }
  return result.toArray(new String[result.size()]);
}
