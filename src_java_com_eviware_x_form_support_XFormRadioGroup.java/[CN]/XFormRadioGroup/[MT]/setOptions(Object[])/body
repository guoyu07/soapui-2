{
  while (buttonGroup.getButtonCount() > 0)   buttonGroup.remove(buttonGroup.getElements().nextElement());
  models.clear();
  items.clear();
  getComponent().removeAll();
  for (  Object value : values) {
    addItem(value.toString());
  }
}
