{
  ComboBoxModel model=getComponent().getModel();
  String[] result=new String[model.getSize()];
  for (int c=0; c < result.length; c++)   result[c]=model.getElementAt(c).toString();
  return result;
}
