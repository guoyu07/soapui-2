{
  if (name.equals("component")) {
    getComponent().removeAll();
    if (value != null)     getComponent().add((JComponent)value,BorderLayout.CENTER);
  }
 else {
    super.setProperty(name,value);
  }
}
