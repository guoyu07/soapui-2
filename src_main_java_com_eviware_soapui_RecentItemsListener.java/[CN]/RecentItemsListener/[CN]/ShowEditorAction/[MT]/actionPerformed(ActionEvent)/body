{
  ModelItem modelItem=ref.get();
  if (modelItem != null)   UISupport.showDesktopPanel(modelItem);
 else   UISupport.showErrorMessage("Item [" + getValue(Action.NAME) + "] is no longer available");
}
