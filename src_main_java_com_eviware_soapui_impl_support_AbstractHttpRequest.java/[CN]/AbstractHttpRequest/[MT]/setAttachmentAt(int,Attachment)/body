{
  if (attachments.size() > index)   attachments.set(index,(FileAttachment)attachment);
 else   attachments.add((FileAttachment)attachment);
  notifyPropertyChanged(ATTACHMENTS_PROPERTY,null,attachment);
}
