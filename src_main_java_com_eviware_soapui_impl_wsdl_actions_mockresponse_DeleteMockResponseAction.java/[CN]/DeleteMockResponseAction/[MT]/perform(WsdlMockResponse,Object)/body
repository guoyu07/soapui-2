{
  if (UISupport.confirm("Delete MockResponse [" + mockResponse.getName() + "] from MockOperation ["+ mockResponse.getMockOperation().getName()+ "]",getName())) {
    WsdlMockOperation operation=(WsdlMockOperation)mockResponse.getMockOperation();
    operation.removeMockResponse(mockResponse);
  }
}
