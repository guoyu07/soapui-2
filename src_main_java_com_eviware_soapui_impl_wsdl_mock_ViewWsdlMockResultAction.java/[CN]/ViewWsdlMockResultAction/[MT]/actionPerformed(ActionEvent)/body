{
  try {
    if (result.isDiscarded())     UISupport.showInfoMessage("Request has been discarded..");
 else     UISupport.showDesktopPanel(buildFrame());
  }
 catch (  Exception ex) {
    SoapUI.logError(ex);
  }
}
