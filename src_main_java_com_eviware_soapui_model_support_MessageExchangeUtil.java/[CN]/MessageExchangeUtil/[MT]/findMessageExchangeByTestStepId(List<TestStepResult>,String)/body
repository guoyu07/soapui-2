{
  for (  TestStepResult tsr : results) {
    String id=tsr.getTestStep().getId();
    if (id.equals(testStepId) && tsr instanceof MessageExchange)     return (MessageExchange)tsr;
  }
  return null;
}
