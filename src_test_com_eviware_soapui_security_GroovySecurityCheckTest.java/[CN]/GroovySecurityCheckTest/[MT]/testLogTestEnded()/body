{
  SecurityTestRunnerImpl testRunner=new SecurityTestRunnerImpl(createSecurityTest());
  testRunner.start(false);
  assertTrue("Groovy Security Check Failed due to wrong Log message",testRunner.getSecurityTest().getSecurityTestLog().getElementAt(0).getMessage().startsWith("SecurityTest ended"));
}
