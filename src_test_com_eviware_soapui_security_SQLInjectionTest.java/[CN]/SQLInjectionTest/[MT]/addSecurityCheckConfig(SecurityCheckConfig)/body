{
  SQLInjectionCheck sqlCheck=(SQLInjectionCheck)SecurityCheckRegistry.getInstance().getFactory(securityCheckType).buildSecurityCheck(securityCheckConfig,null);
  List<String> params=new ArrayList<String>();
  params.add("q");
}
