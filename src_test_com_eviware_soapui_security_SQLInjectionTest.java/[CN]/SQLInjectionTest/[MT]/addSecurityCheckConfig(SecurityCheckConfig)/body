{
  SQLInjectionCheck sqlCheck=(SQLInjectionCheck)SecurityCheckRegistry.getInstance().getFactory(securityCheckType).buildSecurityCheck(securityCheckConfig);
  List<String> params=new ArrayList<String>();
  params.add("q");
}
