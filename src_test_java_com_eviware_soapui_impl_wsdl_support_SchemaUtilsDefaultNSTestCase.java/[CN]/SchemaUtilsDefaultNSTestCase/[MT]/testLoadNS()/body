{
  SoapUI.initDefaultCore();
  File file=new File("src" + File.separator + "test-resources"+ File.separator+ "chameleon"+ File.separator+ "chameleon.wsdl");
  SchemaTypeSystem sts=SchemaUtils.loadSchemaTypes(file.toURI().toURL().toString(),new UrlWsdlLoader(file.toURI().toURL().toString()));
  assertNotNull(sts);
}
